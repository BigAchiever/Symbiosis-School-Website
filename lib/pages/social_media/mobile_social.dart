import 'package:flutter/material.dart';

class MobileSocialMedia extends StatelessWidget {
  const MobileSocialMedia({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}